# Core Libraries
streamlit>=1.27.0  # For the Streamlit UI
pandas>=1.5.0      # Data manipulation
numpy>=1.23.0      # Numerical computations
yfinance>=0.2.0    # Fetching stock data (used for fundamentals)
ta>=0.10.0         # Technical analysis indicators
plotly>=5.10.0     # Interactive charts
requests>=2.28.0   # HTTP requests for NSE stock list and news API

# Sentiment Analysis
vaderSentiment>=3.3.2        # VADER sentiment analysis
transformers>=4.30.0         # FinBERT sentiment analysis
torch>=2.0.0                 # Required for transformers (FinBERT)
spacy>=3.5.0                 # Entity extraction with spaCy
pytrends>=4.9.0              # Google Trends for trending stocks
beautifulsoup4>=4.11.0       # Parsing HTML (potentially useful for news scraping)
textblob>=0.17.0             # Additional sentiment analysis (optional)
nltk>=3.8.0                  # Text processing (optional for sentiment)

# Performance and Retry Logic
aiohttp>=3.8.0       # Asynchronous HTTP requests for stock data
tenacity>=8.2.0      # Retry logic (alternative to custom retry decorator, optional)
more-itertools>=9.0.0 # Enhanced itertools functionality (used in update_progress)

# Monte Carlo with GARCH
arch>=5.3.0          # GARCH modeling for Monte Carlo simulation

# Utilities
tqdm>=4.65.0         # Progress bars (used in batch processing)
python-dateutil>=2.8.0  # Date handling (implicitly used by pandas/yfinance)
diskcache>=5.4.0     # Caching (optional enhancement for fetch_stock_data_cached)

# Optional Visualization (not strictly required but potentially useful)
matplotlib>=3.7.0    # Additional plotting (optional for custom visualizations)